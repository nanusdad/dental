import {
	Meteor
} from 'meteor/meteor';

import '../imports/api/assistants.js';
import '../imports/api/clinics.js';
import '../imports/api/procedures.js';

Meteor.startup(() => {
	// code to run on server at startup
});